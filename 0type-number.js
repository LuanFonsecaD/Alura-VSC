// Tipo Número:

const meuNumero = 3;

const primeiroNumero = 1;
const segundoNumero = 2;

const operacaoMatematica = primeiroNumero - segundoNumero;

console.log(operacaoMatematica)

// Ponto Flutuante

const numeroPontoFlutuante = 3.3;
const numeroFlutuanteSemZero = .5;

const novaOperacao = numeroFlutuanteSemZero + primeiroNumero;

console.log(novaOperacao)

// NaN = Not a Number:

const alura = "Alura"

console.log(alura * primeiroNumero)

// Número Pi:

const raio = 1;
const areaCirculo = Math.PI * raio * raio;

console.log(areaCirculo)

// Expoente e Notação Científica:

const expoenteNum = 2e+2;

console.log(expoenteNum)

// Divisão por zero:

var a = 10
var b = 0
console.log(a/b) // Infinity

var a = 0
var b = 0
console.log(a/b) // NaN
